# SweetDisplayHost plan

Initial console host: discover private driver interface → overlapped WAIT_FRAME requests → bounded frame queue → raw/debug or Media Foundation encoder → pluggable transport → structured telemetry. No driver-to-host implementation exists yet. First bring-up must prove real indirect-display pixels, not synthesized input. See docs/DISPLAY_PIPELINE.md.
